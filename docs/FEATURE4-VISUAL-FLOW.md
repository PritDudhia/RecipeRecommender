# Feature 4: Cuisine Classification - Visual Flow

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CUISINE CLASSIFICATION                       │
│                              (k-NN Based)                            │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│   Frontend   │ ───▶ │   Flask API  │ ───▶ │   k-NN Model │
│  (React UI)  │ ◀─── │  (Backend)   │ ◀─── │  (sklearn)   │
└──────────────┘      └──────────────┘      └──────────────┘
                              │
                              ▼
                      ┌──────────────┐
                      │ Recipe DB    │
                      │ 120+ recipes │
                      │ 32 cuisines  │
                      └──────────────┘
```

## Detailed Component Flow

### 1. Data Pipeline

```
┌────────────────────────────────────────────────────────────────┐
│                      DATA PREPARATION                           │
└────────────────────────────────────────────────────────────────┘

world_recipes_data.py
        │
        ├─▶ Recipe 1: {"name": "Margherita Pizza",
        │              "cuisine": "Italian",
        │              "ingredients": ["tomato", "basil", "mozzarella"]}
        │
        ├─▶ Recipe 2: {"name": "Kung Pao Chicken",
        │              "cuisine": "Chinese",
        │              "ingredients": ["chicken", "peanuts", "soy sauce"]}
        │
        └─▶ ... (120+ recipes)

                    ▼

┌───────────────────────────────────────────────────────────────┐
│                   INGREDIENT EXTRACTION                        │
└───────────────────────────────────────────────────────────────┘

For each recipe:
    ingredients_str = " ".join(recipe['ingredients'])

Example outputs:
    - "tomato basil mozzarella olive oil garlic"
    - "chicken peanuts soy sauce ginger garlic"
    - "rice curry powder turmeric cumin onion"

                    ▼

┌───────────────────────────────────────────────────────────────┐
│                   TF-IDF VECTORIZATION                         │
└───────────────────────────────────────────────────────────────┘

TfidfVectorizer converts text to numerical vectors:

Input:  ["tomato basil mozzarella", "rice curry turmeric"]

Output: Sparse Matrix (120 × 200+)
        ┌─────┬───────┬────────┬──────┬─────┬─────┐
        │  🍅  │  🌿   │  🧀    │  🍚  │ 🍛  │ ... │
        ├─────┼───────┼────────┼──────┼─────┼─────┤
Recipe1 │ 0.65│ 0.52  │  0.48  │ 0.00 │0.00 │ ... │
Recipe2 │ 0.00│ 0.00  │  0.00  │ 0.71 │0.68 │ ... │
        └─────┴───────┴────────┴──────┴─────┴─────┘
```

### 2. Model Training Flow

```
┌────────────────────────────────────────────────────────────────┐
│                       MODEL TRAINING                            │
└────────────────────────────────────────────────────────────────┘

Step 1: Load Recipes
    ┌──────────────────────┐
    │ get_world_recipes()  │ ──▶ 120+ recipes
    └──────────────────────┘

Step 2: Extract Features
    ┌──────────────────────┐
    │ TF-IDF Vectorizer    │ ──▶ Convert to vectors
    └──────────────────────┘

Step 3: Fit k-NN Model
    ┌──────────────────────────────┐
    │ NearestNeighbors(            │
    │   n_neighbors=5,             │
    │   metric='cosine'            │
    │ )                            │
    └──────────────────────────────┘
              │
              ▼
    ┌──────────────────────┐
    │ knn.fit(X_tfidf)     │
    └──────────────────────┘

Step 4: Store Labels
    ┌──────────────────────┐
    │ cuisine_labels list  │ ──▶ Match with recipes
    └──────────────────────┘
```

### 3. Prediction Pipeline

```
┌────────────────────────────────────────────────────────────────┐
│                    PREDICTION WORKFLOW                          │
└────────────────────────────────────────────────────────────────┘

User Input: ["tomato", "garlic", "basil", "mozzarella", "olive oil"]
                               │
                               ▼
                    ┌──────────────────────┐
                    │ Join ingredients     │
                    │ into single string   │
                    └──────────────────────┘
                               │
                "tomato garlic basil mozzarella olive oil"
                               │
                               ▼
                    ┌──────────────────────┐
                    │ TF-IDF Transform     │
                    │ (use trained vocab)  │
                    └──────────────────────┘
                               │
                    [0.45, 0.33, 0.52, ...]  ← Vector
                               │
                               ▼
                    ┌──────────────────────┐
                    │ Find 5 Nearest       │
                    │ Neighbors (cosine)   │
                    └──────────────────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        ▼                      ▼                      ▼
   Recipe A              Recipe B              Recipe C
   Italian               Italian               Mediterranean
   (distance: 0.12)      (distance: 0.18)      (distance: 0.22)
        
        ▼                      ▼                      ▼
   Recipe D              Recipe E
   Italian               Greek
   (distance: 0.25)      (distance: 0.30)
                               │
                               ▼
                    ┌──────────────────────┐
                    │ Count Cuisines:      │
                    │ Italian: 3           │
                    │ Mediterranean: 1     │
                    │ Greek: 1             │
                    └──────────────────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │ Calculate Confidence │
                    │ Italian: 3/5 = 0.60  │
                    │ Mediterranean: 0.20  │
                    │ Greek: 0.20          │
                    └──────────────────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │ Return top 3 results │
                    └──────────────────────┘
```

### 4. API Request Flow

```
┌────────────────────────────────────────────────────────────────┐
│                      API INTERACTION                            │
└────────────────────────────────────────────────────────────────┘

Frontend (React)
    │
    │ User enters: "tomato, garlic, basil, mozzarella"
    │
    ▼
┌──────────────────────────────────────────────────────┐
│ POST /api/cuisine/classify                           │
│ Body: {                                              │
│   "ingredients": ["tomato", "garlic", "basil",       │
│                   "mozzarella", "olive oil"]         │
│ }                                                    │
└──────────────────────────────────────────────────────┘
    │
    ▼
Flask Backend
    │
    ├─▶ Validate input
    │   └─ Check: ingredients is list, not empty
    │
    ├─▶ Call classifier.classify(ingredients, top_n=3)
    │
    ▼
CuisineClassifier Model
    │
    ├─▶ Vectorize input ingredients
    │
    ├─▶ Find k=5 nearest neighbors
    │
    ├─▶ Count cuisine occurrences
    │
    └─▶ Calculate confidence scores
    │
    ▼
Backend Response
┌──────────────────────────────────────────────────────┐
│ Response: {                                          │
│   "success": true,                                   │
│   "predictions": [                                   │
│     {"cuisine": "Italian", "confidence": 0.85},      │
│     {"cuisine": "Mediterranean", "confidence": 0.10},│
│     {"cuisine": "Greek", "confidence": 0.05}         │
│   ],                                                 │
│   "ingredient_count": 5                              │
│ }                                                    │
└──────────────────────────────────────────────────────┘
    │
    ▼
Frontend Display
    │
    ├─▶ Show top cuisine: "Italian (85%)"
    │
    ├─▶ Show 2nd: "Mediterranean (10%)"
    │
    └─▶ Show 3rd: "Greek (5%)"
```

### 5. Distance Calculation (Cosine Similarity)

```
┌────────────────────────────────────────────────────────────────┐
│                   COSINE SIMILARITY METRIC                      │
└────────────────────────────────────────────────────────────────┘

Given two ingredient vectors A and B:

Vector A (User Input):
    [0.45, 0.33, 0.52, 0.00, 0.19, ...]
         ↓
    tomato basil mozzarella

Vector B (Recipe in DB):
    [0.51, 0.29, 0.48, 0.00, 0.22, ...]
         ↓
    tomato basil mozzarella oregano

Cosine Distance = 1 - (A · B) / (||A|| × ||B||)

Where:
    A · B = dot product
    ||A|| = magnitude of A
    ||B|| = magnitude of B

Lower distance = More similar
Higher distance = Less similar

Example:
    Italian Pizza Recipe: distance = 0.12 ✓ (very similar)
    Chinese Stir-fry:     distance = 0.89 ✗ (very different)
```

### 6. Frontend Component Structure

```
┌────────────────────────────────────────────────────────────────┐
│                    REACT COMPONENT FLOW                         │
└────────────────────────────────────────────────────────────────┘

App.jsx
    │
    ├─▶ State Management
    │   ├─ cuisineIngredients (user input)
    │   └─ cuisinePredictions (API results)
    │
    ├─▶ classifyCuisine() function
    │   ├─ Split ingredients by newline
    │   ├─ Filter empty strings
    │   ├─ POST to /api/cuisine/classify
    │   └─ Update cuisinePredictions state
    │
    └─▶ UI Rendering
        ├─ Textarea for ingredient input
        ├─ "Classify Cuisine" button
        └─ Results display
            ├─ Cuisine name
            ├─ Confidence percentage
            └─ Visual confidence bar

User Interaction:
    Type ingredients ──▶ Click "Classify" ──▶ See results
         │                      │                   │
         ▼                      ▼                   ▼
    "tomato"              API call            "Italian 85%"
    "basil"               to Flask            [████████░░]
    "mozzarella"          backend
```

### 7. Error Handling Flow

```
┌────────────────────────────────────────────────────────────────┐
│                     ERROR HANDLING                              │
└────────────────────────────────────────────────────────────────┘

User Input Validation:
    │
    ├─▶ Empty ingredients?
    │   └─ Return error: "Please provide ingredients"
    │
    ├─▶ Invalid format?
    │   └─ Return error: "Ingredients must be a list"
    │
    └─▶ Model not trained?
        └─ Auto-train on first request

API Error Responses:
    │
    ├─▶ 400 Bad Request
    │   └─ Invalid input format
    │
    ├─▶ 500 Internal Error
    │   └─ Model training failed
    │
    └─▶ 200 Success
        └─ Valid predictions returned

Frontend Error Display:
    │
    └─▶ Show error message in UI
        └─ "Unable to classify. Please try again."
```

## Data Flow Summary

```
┌─────────────┐
│   Recipes   │ (120+ from world_recipes_data.py)
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   TF-IDF    │ (Vectorize ingredients)
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   k-NN Fit  │ (Train with k=5, cosine metric)
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ User Input  │ (New ingredient list)
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Transform  │ (Apply TF-IDF)
└──────┬──────┘
       │
       ▼
┌─────────────┐
│Find k=5 NN  │ (Nearest neighbors)
└──────┬──────┘
       │
       ▼
┌─────────────┐
│Count Votes  │ (Cuisine occurrences)
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Top 3 Results│ (With confidence scores)
└─────────────┘
```

## Performance Metrics

```
Model Characteristics:
    ┌──────────────────────────────┐
    │ Recipes: 120+                │
    │ Cuisines: 32                 │
    │ Features: 200+ ingredients   │
    │ k-value: 5 neighbors         │
    │ Metric: Cosine similarity    │
    │ Prediction time: < 50ms      │
    └──────────────────────────────┘

Prediction Quality:
    High Confidence (> 60%): Distinctive cuisines
    Medium Confidence (30-60%): Fusion or similar
    Low Confidence (< 30%): Ambiguous patterns
```

## Example: Step-by-Step Prediction

```
Input: ["tomato", "basil", "mozzarella", "olive oil"]

Step 1: Vectorize
    ──▶ [0.45, 0.33, 0.52, 0.19, 0.00, ...]

Step 2: Find Neighbors (distances shown)
    ──▶ Margherita Pizza (Italian)      [0.08]  ✓
    ──▶ Caprese Salad (Italian)         [0.12]  ✓
    ──▶ Pasta Pomodoro (Italian)        [0.15]  ✓
    ──▶ Greek Salad (Greek)             [0.28]  ○
    ──▶ Bruschetta (Mediterranean)      [0.31]  ○

Step 3: Count Cuisines
    ──▶ Italian: 3 votes
    ──▶ Greek: 1 vote
    ──▶ Mediterranean: 1 vote

Step 4: Calculate Confidence
    ──▶ Italian: 3/5 = 60%
    ──▶ Greek: 1/5 = 20%
    ──▶ Mediterranean: 1/5 = 20%

Result:
    ┌────────────────────────────┐
    │ Italian          60% ████  │
    │ Greek            20% ██    │
    │ Mediterranean    20% ██    │
    └────────────────────────────┘
```
